Quiz title: Line Graphs
Quiz description: A short quiz on the Line Graphs notebook (DS 2023 | Communicating with Data).

1. Line charts are most appropriate when the x axis consists of:
a) Unique categorical labels
*b) Numeric data on an ordinal or ratio scale (e.g., time)
c) Parts of a whole
d) Two independent numeric variables

2. In Pandas, which plot type is produced by default when you call `.plot()` with no `kind` specified?
*a) A line graph
b) A bar chart
c) A pie chart
d) A scatter plot

3. When is it appropriate to show multiple lines on the same axes?
a) Whenever there is more than one column
*b) When the features share the same domain (e.g., both are prices)
c) Only when the x axis is time
d) Only when using subplots

4. Which Matplotlib function lets you display two different y-axis scales on the same axes?
a) ax.subplots()
*b) ax.twinx()
c) ax.secondary_yaxis()
d) ax.fill_between()

5. According to the notebook, how does a bar chart differ from a line graph in representing the domain?
a) A bar chart truncates the y axis to emphasize the trend
*b) A bar chart treats x-axis values as categories and uses a zero baseline
c) A bar chart requires the x axis to be numeric
d) A bar chart cannot show more than one feature

6. Besides trends over time, what other use for line graphs is demonstrated in the notebook?
a) Showing parts of a whole
*b) Plotting classical mathematical functions such as y = sin(x)
c) Comparing counts of categories
d) Revealing occlusion in dense data
