Quiz title: Bar Charts
Quiz description: A short quiz on the Bar Charts notebook (DS 2023 | Communicating with Data).

1. What does a bar chart primarily represent?
a) The correlation between two numeric variables
*b) Numerical values compared across different categories
c) Parts of a single whole
d) The distribution of a single continuous variable

2. In a standard bar chart, what encodes the numeric value of each category?
a) The width of the bar
*b) The height (or length) of the bar
c) The color of the bar
d) The spacing between bars

3. Which argument to Pandas' `.plot()` method creates a *horizontal* bar chart?
a) kind='bar'
*b) kind='barh'
c) kind='hbar'
d) orientation='horizontal'

4. When plotting the Iris data, why must the `species` feature be grouped and aggregated (e.g., with `.mean()`) before making a bar chart?
a) Because bar charts cannot display text labels
*b) Because the nominal values repeat across observations and each bar needs a single value per category
c) Because Pandas requires the index to be numeric
d) Because the species column contains missing values

5. When calling `iris_means.plot.bar()` on a data frame with multiple numeric columns, what does Pandas do by default?
*a) Draws a grouped bar for each category with one bar per feature
b) Stacks all the features into a single bar per category
c) Creates a separate subplot for each feature automatically
d) Plots only the first numeric column

6. What does setting `stacked=True` do in a bar chart of multiple features?
a) Places each feature in its own subplot
*b) Stacks the feature bars on top of one another within each category
c) Rotates the x-axis labels
d) Removes the legend
